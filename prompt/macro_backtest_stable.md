你现在需要重写并覆盖现有脚本：

build_backtest_macro_stable.py

目标：
在保持现有风控结构不变的前提下，
对参数进行合理化调整，使策略：

1）降低过高现金比例
2）缓解验证期波动率失控
3）保持稳定权重结构
4）保持可汇报性

--------------------------------------------------
【当前版本存在的问题】

- 平均现金比例 ≈ 41%
- 验证期年化波动率 ≈ 3.65%
- 目标波动率设定过低（2%）

当前模型属于“被压死的风险平价”。

--------------------------------------------------
【本次调整目标】

将策略调整为：

- 更合理的目标风险水平
- 更温和的宏观冲击
- 保持风控约束
- 提高资金利用率

--------------------------------------------------
【必须修改的参数】

1）目标年化波动率：

target_vol = 0.05

（原为 0.05，过低）

--------------------------------------------------

2）宏观风险预算冲击幅度：

将所有 ±0.03 或 ±0.05 改为：

macro_tilt = 0.05

示例：
若 growth_signal == strong:
    hs300 += 0.05
    sp500 += 0.05
    cgb10y -= 0.05
    policy_bond -= 0.05

冲击必须保持对称。
调整后重新归一化。

--------------------------------------------------

3）保留以下风控约束（不删除）：

max_weight = 0.40
max_weight_change = 0.20
min_cash = 0.05

--------------------------------------------------
【模型结构保持不变】

必须保留：

- 月度调仓
- 252日协方差窗口
- 动态资产池
- 不使用未来数据
- 不使用机器学习
- 不使用外部优化器

--------------------------------------------------
【风险控制流程顺序】

必须保持以下顺序：

1）基础风险预算
2）宏观冲击
3）风险平价权重计算
4）单资产权重上限
5）单次权重变化限制
6）目标波动率缩放（target_vol = 0.03）
7）最低现金比例约束

--------------------------------------------------
【输出目录保持不变】

回测引擎_macro_stable/

保存：

weights_macro_stable.csv
nav_series_macro_stable.csv
backtest_results_macro_stable.csv

--------------------------------------------------
【终端必须打印】

1）最大单资产权重
2）最大单次权重变化
3）平均现金比例
4）两阶段绩效表
5）两阶段年化波动率是否 <= 0.03

--------------------------------------------------
【代码要求】

- 使用 pandas + numpy
- 所有参数集中写在脚本顶部
- 不要改变文件路径
- 注释清晰
- 不允许硬编码日期

--------------------------------------------------
最终目标效果：

- 平均现金比例 < 25%
- 验证期波动率接近但不超过 3%
- 权重结构平滑
- Sharpe 比率稳定