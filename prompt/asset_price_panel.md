你现在需要为一个多资产风险预算模型构建标准化价格面板。

项目结构如下：

project/ raw_data/ ETF收盘价+成交金额.xlsx 511260+511520指数收盘价.xlsx 511070.SH.xlsx 511070指数.xlsx processed_data/ （如果不存在请自动创建）

任务目标：

生成文件：

processed_data/asset_price_panel.csv

要求如下：

|                                   |
|:----------------------------------|
| 第一步：读取并清洗 ETF 主价格数据 |

文件：ETF收盘价+成交金额.xlsx

仅保留“日期 + 各ETF收盘价”列。 删除成交金额列。

需要保留的资产（并重命名为英文）：

510300.SH → hs300 513500.SH → sp500 511260.SH → cgb10y 518880.SH → gold 511520.SH → policy_bond

日期列统一命名为：date 格式转为 pandas datetime 按日期升序排列

|                                        |
|:---------------------------------------|
| 第二步：补齐 511260 和 511520 早期缺失 |

文件：511260+511520指数收盘价.xlsx

读取两个指数：

-   10年国债全价指数 → 用于补齐 cgb10y
-   政策性金融债全价指数 → 用于补齐 policy_bond

方法：

1）计算指数日收益率 2）找到 ETF 有真实价格的第一个有效日期 3）向前用指数收益率反推构造价格序列 4）拼接为完整价格序列

最终保证：

cgb10y 与 policy_bond 从 2016-01-04 起无缺失

|                                  |
|:---------------------------------|
| 第三步：构建信用债价格（511070） |

需要合并两个文件：

1）511070指数.xlsx 2）511070.SH.xlsx

规则：

-   2022-07-01 之前：不纳入资产池（保留为 NaN，不允许补零）
-   2022-07-01 至 2025-01-21：使用指数价格
-   2025-01-22 以后：使用 ETF 实际收盘价

生成列名：

credit_bond

注意：

严禁补零。 指数为 0 的时期视为无效数据，应设为 NaN。

|                        |
|:-----------------------|
| 第四步：构建统一时间轴 |

时间范围：

2016-01-04 至 最新日期

用所有资产的日期并集作为基准。

最终 DataFrame 列顺序：

date hs300 sp500 cgb10y gold policy_bond credit_bond

按日期升序排列。

|              |
|:-------------|
| 第五步：输出 |

保存为：

processed_data/asset_price_panel.csv

要求：

-   date 列为第一列
-   不包含重复日期
-   不包含字符串格式价格
-   保留 NaN（不要填充）
-   不要进行收益率计算（下一步单独做）

------------------------------------------------------------------------

最后：

打印：

1）数据起始日期 2）数据结束日期 3）每个资产的非空起始日期 4）各列缺失值统计

代码需结构清晰，函数化处理。